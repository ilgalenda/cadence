# Calls Agent — Knowledge Base

Place your knowledge base `.md` files here. These are loaded at runtime to give the Calls agent context about your products, market, and sales narratives.

Files matching `timebeat-*.md` and the `_user/` directory are gitignored and must be created locally.
