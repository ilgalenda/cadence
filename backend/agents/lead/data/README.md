# Lead Agent — Data

Runtime data files for the Lead agent. The following files are gitignored and generated at runtime:

- `campaigns.json` — campaign analysis and prospect intelligence
- `leads.json` — lead database with company and contact data
- `google_tokens.json` — Google OAuth tokens (never commit this)
